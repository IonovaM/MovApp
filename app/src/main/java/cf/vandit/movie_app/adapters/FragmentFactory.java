package cf.vandit.movie_app.adapters;
import androidx.fragment.app.Fragment;


// Абстрактный класс FragmentFactory, определяющий метод создания фрагмента
public abstract class FragmentFactory {
    public abstract Fragment createFragment(); // Абстрактный метод для создания фрагмента
}
